interface:
  display_name: "Writing Plans"
  short_description: "Create a compact executable implementation plan"
  default_prompt: "Use $writing-plans to turn these requirements into an executable plan."

policy:
  allow_implicit_invocation: false
